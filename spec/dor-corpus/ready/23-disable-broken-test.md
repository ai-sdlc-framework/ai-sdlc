## Description
Disable the `scripts/check-publishable-package-configs.mjs` test in `pipeline-cli/src/index.test.ts` until the script is rewritten — track the rewrite as a follow-up issue.

## Acceptance Criteria
- [ ] #1 The test is skipped via `it.skip` with a comment linking to RFC-0011
