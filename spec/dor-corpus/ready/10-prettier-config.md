## Description
Add `printWidth: 100` to `.prettierrc.json` so long type signatures don't wrap aggressively.

## Acceptance Criteria
- [ ] #1 `.prettierrc.json` declares `printWidth: 100`
- [ ] #2 `pnpm format` reports no diff after the change
