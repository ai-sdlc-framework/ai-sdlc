## Description
Tighten the patch-coverage target in `codecov.yml` from 80% to 85% for the `pipeline-cli` flag only.

## Acceptance Criteria
- [ ] #1 `codecov.yml` declares an 85% patch target scoped to `pipeline-cli`
