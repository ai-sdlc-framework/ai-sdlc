## Description
Enable the `@typescript-eslint/no-floating-promises` rule in `eslint.config.mjs`.

## Acceptance Criteria
- [ ] #1 Add the rule to `eslint.config.mjs` at error severity
- [ ] #2 `pnpm lint` runs clean across the workspace
