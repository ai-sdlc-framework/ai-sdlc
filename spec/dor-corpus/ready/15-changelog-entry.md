## Description
Append a "Changed" bullet under `Unreleased` in `CHANGELOG.md` describing the new `--dry-run` flag.

## Acceptance Criteria
- [ ] #1 `CHANGELOG.md` has a new bullet under "Unreleased > Changed"
