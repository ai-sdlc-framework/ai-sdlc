## Description
The `ai-sdlc-pipeline aggregate-verdicts` subcommand help text in `pipeline-cli/src/cli/index.ts` says "verdicts" but the option flag is `--verdicts` plural. Make the help text match.

## Acceptance Criteria
- [ ] #1 Help text in `pipeline-cli/src/cli/index.ts` for the aggregate-verdicts command matches the option flag
