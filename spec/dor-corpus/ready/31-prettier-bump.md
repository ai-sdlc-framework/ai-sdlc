## Description
Bump `prettier` from 3.8.1 to 3.8.2 in `package.json` to pick up the new TypeScript 5.7 type-arg formatting.

## Acceptance Criteria
- [ ] #1 `package.json` declares `prettier: ^3.8.2`
- [ ] #2 `pnpm format` runs clean across the workspace
