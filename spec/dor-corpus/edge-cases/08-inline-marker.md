## Description
The variable `TBD` is the placeholder. Update `pipeline-cli/src/dor/evaluate.ts` to remove it.

## Acceptance Criteria
- [ ] #1 `pipeline-cli/src/dor/evaluate.ts` no longer references the variable
