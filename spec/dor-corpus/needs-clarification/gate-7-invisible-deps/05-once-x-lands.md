## Description
Wire up `pipeline-cli/src/runtime/exec.ts` to the new harness once the harness rewrite lands.

## Acceptance Criteria
- [ ] #1 `pipeline-cli/src/runtime/exec.ts` uses the new harness API
