apiVersion: ai-sdlc.io/v1alpha1
kind: Pipeline
metadata:
  name: minimal-pipeline
spec:
  triggers:
    - event: issue.assigned
  providers:
    issueTracker:
      type: linear
  stages:
    - name: implement
